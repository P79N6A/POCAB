import { OnInit, EventEmitter, OnChanges, SimpleChanges } from '@angular/core';
import { NgbInputDatepicker, NgbDate } from '@ng-bootstrap/ng-bootstrap';
import { DateRange } from '../models/date-range';
import { NgbDateNativeAdapter } from '../services/NgbDateNativeAdapter';
export declare class DateRangePickerComponent implements OnInit, OnChanges {
    private readonly dateAdapter;
    dateRange: DateRange;
    minDate?: Date;
    maxDate?: Date;
    disabled?: boolean;
    dateRangeChange: EventEmitter<DateRange>;
    hoveredDate: NgbDate;
    private fromDate;
    private toDate;
    private min;
    private max;
    private inputElRef;
    private dp;
    constructor(dateAdapter: NgbDateNativeAdapter);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    onDateChange(date: NgbDate, dp: NgbInputDatepicker): void;
    private formatInputText;
    isHovered(date: any): any;
    isInside: (date: any) => any;
    isFrom: (date: any) => any;
    isTo: (date: any) => any;
    isWeekend(date: NgbDate): boolean;
    isDisabled: (date: any) => any;
    isInFuture: (date: any) => any;
}
